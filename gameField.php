
<?php
for ($row = 1; $row <= 3; $row++) {
    echo "<tr>";
    for ($col = 1; $col <= 3; $col++) {
        echo "<td>";
        echo "<input type='button' value=''>";
        echo "</td>";
    }
    echo "</tr>";
}
?>
<!-- spielfeld einen namen geben(array)
 Beim klicken des Felds soll der wert X oder O vergeben werden ohne dass das ganze Feld restaret wird sondern nur die felder aktualisiert
 var spielfeld= [" "," "," ",] -->

 <!-- wie spreche ich $row an 
 wie spreche ich $col an -->
 <!-- google suche, php matrix, button und verarbeiten php nutzen -->
    <!-- wie kann ich die werte der buttons verändern -->
    <!-- wie kann ich die werte der buttons verändern ohne dass die seite neu geladen wird -->